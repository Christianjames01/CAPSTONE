# Copy to .env and fill in from Supabase > Project Settings > API.
# Only public values belong here -- anything starting with VITE_ is
# bundled into the website. Never put the service_role/secret key,
# database passwords, or email passwords in this file.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
